diff --git a/CMakeLists.txt b/CMakeLists.txt
index caea48c..cde20f1 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -33,6 +33,10 @@ endif()
 
 option(LLAMA_USE_SYSTEM_GGML "Use system libggml" OFF)
 
+# Shannon-Prime VHT2 KV cache compression integration (off by default).
+option(LLAMA_SHANNON_PRIME "llama: enable Shannon-Prime VHT2 KV cache compression" OFF)
+set(SHANNON_PRIME_DIR "" CACHE STRING "Path to shannon-prime-llama repo (with lib/shannon-prime submodule). Required when LLAMA_SHANNON_PRIME=ON.")
+
 option(LLAMA_WASM_MEM64 "llama: use 64-bit memory in WASM builds" ON)
 
 if (EMSCRIPTEN)
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 121c21f..3b2b68b 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -163,6 +163,43 @@ target_compile_features   (llama PRIVATE cxx_std_17) # don't bump
 
 target_link_libraries(llama PUBLIC ggml)
 
+if (LLAMA_SHANNON_PRIME)
+    if (NOT SHANNON_PRIME_DIR)
+        message(FATAL_ERROR "LLAMA_SHANNON_PRIME=ON requires -DSHANNON_PRIME_DIR=<path/to/shannon-prime-llama>")
+    endif()
+    if (NOT EXISTS "${SHANNON_PRIME_DIR}/lib/shannon-prime/core/shannon_prime.c")
+        message(FATAL_ERROR "SHANNON_PRIME_DIR='${SHANNON_PRIME_DIR}' does not contain lib/shannon-prime/core/shannon_prime.c — did you `git submodule update --init` in shannon-prime-llama?")
+    endif()
+
+    set(_SP_SOURCES
+        ${SHANNON_PRIME_DIR}/lib/shannon-prime/core/shannon_prime.c
+        ${SHANNON_PRIME_DIR}/lib/shannon-prime/tools/shannon_prime_llama.c
+    )
+    set(_SP_DEFS LLAMA_SHANNON_PRIME)
+
+    # Pull in the Adreno (ARM NEON) backend on aarch64 builds so the bridge
+    # can pick SP_BACKEND_ADRENO at runtime (SHANNON_PRIME_BACKEND=adreno env).
+    if (ANDROID OR CMAKE_SYSTEM_PROCESSOR MATCHES "aarch64|arm64")
+        list(APPEND _SP_SOURCES
+            ${SHANNON_PRIME_DIR}/lib/shannon-prime/backends/adreno/shannon_prime_adreno.c)
+        list(APPEND _SP_DEFS SP_HAVE_ADRENO)
+        message(STATUS "Shannon-Prime: linking Adreno NEON backend (SP_HAVE_ADRENO)")
+    endif()
+
+    add_library(shannon_prime_core STATIC ${_SP_SOURCES})
+    target_include_directories(shannon_prime_core PUBLIC
+        ${SHANNON_PRIME_DIR}/lib/shannon-prime/core
+        ${SHANNON_PRIME_DIR}/lib/shannon-prime/tools
+        ${SHANNON_PRIME_DIR}/lib/shannon-prime/backends/adreno
+    )
+    target_compile_definitions(shannon_prime_core PUBLIC ${_SP_DEFS})
+    set_property(TARGET shannon_prime_core PROPERTY POSITION_INDEPENDENT_CODE ON)
+
+    target_compile_definitions(llama PRIVATE ${_SP_DEFS})
+    target_sources(llama PRIVATE llama-shannon-prime.cpp)
+    target_link_libraries(llama PRIVATE shannon_prime_core)
+endif()
+
 if (BUILD_SHARED_LIBS)
     set_target_properties(llama PROPERTIES POSITION_INDEPENDENT_CODE ON)
     target_compile_definitions(llama PRIVATE LLAMA_BUILD)
diff --git a/src/llama-context.cpp b/src/llama-context.cpp
index ee0c292..1847523 100644
--- a/src/llama-context.cpp
+++ b/src/llama-context.cpp
@@ -10,6 +10,9 @@
 #include "llama-model.h"
 #include "llama-ext.h"
 #include "llama.h"
+#ifdef LLAMA_SHANNON_PRIME
+#include "llama-shannon-prime.h"
+#endif
 
 #include <cinttypes>
 #include <cmath>
@@ -364,9 +367,16 @@ llama_context::llama_context(
             sampling.token_ids_full_vocab[i] = i;
         }
     }
+
+#ifdef LLAMA_SHANNON_PRIME
+    llama_sp_maybe_init(&model, (int)cparams.n_ctx);
+#endif
 }
 
 llama_context::~llama_context() {
+#ifdef LLAMA_SHANNON_PRIME
+    llama_sp_free();
+#endif
     if (!model.hparams.no_alloc) {
         for (size_t i = 0; i < backend_ptrs.size(); ++i) {
             ggml_backend_t             backend = backend_ptrs[i];
@@ -1197,6 +1207,10 @@ llm_graph_result * llama_context::process_ubatch(const llama_ubatch & ubatch, ll
         res->reset();
 
         ggml_backend_sched_reset(sched.get());
+        // Shannon-Prime's hook is post-graph_compute, not an eval callback,
+        // because installing any eval_callback forces per-tensor scheduler
+        // sync (killed mobile throughput). Preserve the caller's cb_eval
+        // unchanged in both paths.
         ggml_backend_sched_set_eval_callback(sched.get(), cparams.cb_eval, cparams.cb_eval_user_data);
 
         //const auto t_start_us = ggml_time_us();
@@ -1235,6 +1249,15 @@ llm_graph_result * llama_context::process_ubatch(const llama_ubatch & ubatch, ll
         return nullptr;
     }
 
+#ifdef LLAMA_SHANNON_PRIME
+    // Post-decode Shannon-Prime round-trip. Walks freshly-written KV slots
+    // in the cache and compresses→decompresses in place. One call per
+    // ubatch — no per-tensor scheduler sync.
+    if (llama_sp_is_enabled()) {
+        llama_sp_post_compute(mctx, &ubatch);
+    }
+#endif
+
     ret = GGML_STATUS_SUCCESS;
 
     return res;
diff --git a/src/llama-kv-cache.h b/src/llama-kv-cache.h
index 0b62dc7..b01d22e 100644
--- a/src/llama-kv-cache.h
+++ b/src/llama-kv-cache.h
@@ -208,10 +208,8 @@ public:
     void set_input_k_rot(ggml_tensor * dst) const;
     void set_input_v_rot(ggml_tensor * dst) const;
 
-private:
-    const llama_model & model;
-    const llama_hparams & hparams;
-
+    // kv_layer is public-accessible so the Shannon-Prime post-decode hook
+    // can walk the per-layer K/V tensor storage after graph_compute.
     struct kv_layer {
         // layer index in the model
         // note: can be different from the layer index in the KV cache
@@ -224,6 +222,15 @@ private:
         std::vector<ggml_tensor *> v_stream;
     };
 
+    // Post-decode accessor. Returns the per-kv-layer backing tensors so
+    // out-of-graph hooks (e.g. Shannon-Prime VHT2 round-trip) can iterate
+    // freshly-written slots without needing friend access.
+    const std::vector<kv_layer> & get_layers() const { return layers; }
+
+private:
+    const llama_model & model;
+    const llama_hparams & hparams;
+
     bool v_trans = true;  // the value tensor is transposed
 
     const uint32_t n_seq_max = 1;
@@ -385,6 +392,12 @@ public:
     void set_input_k_rot(ggml_tensor * dst) const;
     void set_input_v_rot(ggml_tensor * dst) const;
 
+    // Post-decode hook accessors — used by Shannon-Prime's out-of-graph
+    // round-trip pass which needs both the cache object and the slot
+    // mapping for the just-processed ubatch.
+    llama_kv_cache * get_kv() const { return kv; }
+    const llama_kv_cache::slot_info & get_cur_sinfo() const { return sinfos[i_cur]; }
+
 private:
     llama_memory_status status;
 
diff --git a/src/llama-shannon-prime.cpp b/src/llama-shannon-prime.cpp
new file mode 100644
index 0000000..3cb8a19
--- /dev/null
+++ b/src/llama-shannon-prime.cpp
@@ -0,0 +1,238 @@
+// Shannon-Prime VHT2 KV cache compression hooks for llama.cpp. See header.
+
+#ifdef LLAMA_SHANNON_PRIME
+
+#include "llama-shannon-prime.h"
+
+#include "llama-model.h"
+#include "llama-hparams.h"
+#include "llama-impl.h"
+#include "llama-kv-cache.h"
+#include "llama-batch.h"
+#include "llama-memory.h"
+
+#include "shannon_prime_llama.h"
+
+#include "ggml.h"
+
+#include <atomic>
+#include <cstdio>
+#include <cstdlib>
+#include <cstring>
+#include <vector>
+
+namespace {
+
+struct sp_state {
+    sp_llama_ctx_t * ctx = nullptr;
+    int head_dim_k = 0;
+    int head_dim_v = 0;
+    int n_layers   = 0;
+    int n_heads_kv = 0;
+};
+
+sp_state g_sp;
+
+// Two thread-local (really decode-thread-local; the hook runs on the
+// same thread that called llama_decode) scratch buffers for the
+// fp16<->fp32 conversion on the post-decode path.
+thread_local std::vector<float> tl_gather;
+thread_local std::vector<float> tl_recon;
+
+inline float * get_scratch(std::vector<float> & v, size_t n) {
+    if (v.size() < n) v.resize(n);
+    return v.data();
+}
+
+// Gather hd floats from cache storage at byte offset `off` (type-aware).
+// Returns true on success.
+inline bool gather_vec(const uint8_t * base, size_t off, int hd,
+                        ggml_type type, float * out) {
+    if (type == GGML_TYPE_F32) {
+        std::memcpy(out, base + off, (size_t)hd * sizeof(float));
+        return true;
+    }
+    if (type == GGML_TYPE_F16) {
+        ggml_fp16_to_fp32_row((const ggml_fp16_t *)(base + off), out, hd);
+        return true;
+    }
+    return false;
+}
+
+inline bool scatter_vec(uint8_t * base, size_t off, int hd,
+                         ggml_type type, const float * in) {
+    if (type == GGML_TYPE_F32) {
+        std::memcpy(base + off, in, (size_t)hd * sizeof(float));
+        return true;
+    }
+    if (type == GGML_TYPE_F16) {
+        ggml_fp32_to_fp16_row(in, (ggml_fp16_t *)(base + off), hd);
+        return true;
+    }
+    return false;
+}
+
+} // namespace
+
+extern "C" void llama_sp_maybe_init(const struct llama_model * model, int n_ctx) {
+    if (g_sp.ctx != nullptr) return;
+    if (model == nullptr) return;
+
+    const auto & hp = model->hparams;
+
+    sp_llama_params_t params = {};
+    params.head_dim    = (int)hp.n_embd_head_k_full;
+    params.n_layers    = (int)hp.n_layer;
+    params.n_heads_kv  = (int)hp.n_head_kv(0);
+    params.max_seq_len = n_ctx > 16 ? n_ctx : 16;
+    params.backend     = decltype(params.backend)(0); // SP_BACKEND_CPU; env override in sp_llama_init
+    params.gpu_context = nullptr;
+
+    if (params.head_dim <= 0 || params.n_layers <= 0 || params.n_heads_kv <= 0) {
+        std::fprintf(stderr,
+            "[Shannon-Prime] skipping init: invalid hparams (hd=%d nl=%d nhkv=%d)\n",
+            params.head_dim, params.n_layers, params.n_heads_kv);
+        return;
+    }
+
+    g_sp.ctx = sp_llama_init(&params);
+    if (g_sp.ctx == nullptr) return;
+
+    g_sp.head_dim_k = params.head_dim;
+    g_sp.head_dim_v = (int)hp.n_embd_head_v_full;
+    g_sp.n_layers   = params.n_layers;
+    g_sp.n_heads_kv = params.n_heads_kv;
+
+    std::fprintf(stderr,
+        "[Shannon-Prime] enabled: head_dim_k=%d head_dim_v=%d n_layers=%d n_heads_kv=%d n_ctx=%d\n",
+        g_sp.head_dim_k, g_sp.head_dim_v, g_sp.n_layers, g_sp.n_heads_kv, params.max_seq_len);
+}
+
+extern "C" void llama_sp_free(void) {
+    if (g_sp.ctx) {
+        sp_llama_free(g_sp.ctx);
+        g_sp.ctx = nullptr;
+    }
+}
+
+extern "C" bool llama_sp_is_enabled(void) {
+    return g_sp.ctx != nullptr;
+}
+
+extern "C" void llama_sp_post_compute(const struct llama_memory_context_i * mctx,
+                                      const struct llama_ubatch * ubatch) {
+    if (!g_sp.ctx || !mctx || !ubatch) return;
+
+    // Only the unified KV cache path is handled. ISWA / hybrid caches are
+    // out of scope for the first post-decode hook iteration.
+    auto * kvc = dynamic_cast<const llama_kv_cache_context *>(mctx);
+    if (!kvc) return;
+
+    auto * kv = kvc->get_kv();
+    if (!kv) return;
+
+    const auto & sinfo  = kvc->get_cur_sinfo();
+    const auto & layers = kv->get_layers();
+    const int n_tokens  = (int)ubatch->n_tokens;
+    if (n_tokens <= 0) return;
+    if (sinfo.idxs.empty() || sinfo.idxs[0].empty()) return;
+
+    const int hd_k = g_sp.head_dim_k;
+    const int hd_v = g_sp.head_dim_v;
+    const int nhk  = g_sp.n_heads_kv;
+
+    float * gather = get_scratch(tl_gather, (size_t)(hd_k > hd_v ? hd_k : hd_v));
+    float * recon  = get_scratch(tl_recon,  (size_t)(hd_k > hd_v ? hd_k : hd_v));
+
+    // Walk each kv layer × each newly-written position × each head.
+    // Round-trip K always. Round-trip V only when v_trans=false so the
+    // per-position per-head vector is contiguous in memory. When v_trans
+    // is true (default), V lives in [head_dim, head, position] order with
+    // position innermost — gather/scatter would need strided access per
+    // element; deferred.
+    for (size_t ikv = 0; ikv < layers.size(); ++ikv) {
+        const auto & L = layers[ikv];
+        ggml_tensor * k = L.k;
+        ggml_tensor * v = L.v;
+        if (!k || !k->data) continue;
+
+        const ggml_type tk = k->type;
+        const size_t    ek = ggml_type_size(tk);
+        const size_t    n_embd_k_gqa = (size_t)k->ne[0]; // head_dim * nhk
+        // Defensive: if the cache's head layout doesn't match what we
+        // initialised against, skip this layer rather than crash.
+        if (n_embd_k_gqa != (size_t)hd_k * (size_t)nhk) continue;
+        const size_t k_pos_stride = n_embd_k_gqa * ek;
+
+        const int il = (int)L.il;
+
+        // Slot 0 in the shadow cache is scratch for round-trip only (we
+        // don't use it as a long-term store here — the live KV cache IS
+        // the store; Shannon-Prime only lends its compress/decompress).
+        uint8_t * kdata = (uint8_t *)k->data;
+
+        for (int t = 0; t < n_tokens; ++t) {
+            uint32_t slot = sinfo.idxs[0][t];
+            const size_t pos_off = (size_t)slot * k_pos_stride;
+
+            for (int h = 0; h < nhk; ++h) {
+                const size_t off = pos_off + (size_t)h * hd_k * ek;
+                if (!gather_vec(kdata, off, hd_k, tk, gather)) continue;
+
+                sp_llama_write_k(g_sp.ctx, il, h, 0, gather);
+                sp_llama_read_k (g_sp.ctx, il, h, 0, recon);
+
+                scatter_vec(kdata, off, hd_k, tk, recon);
+            }
+        }
+
+        // V — only when not transposed. The adjacency check matches
+        // llama_kv_cache::get_v's v_trans=false branch.
+        if (v && v->data) {
+            const ggml_type tv = v->type;
+            const size_t    ev = ggml_type_size(tv);
+            // When v_trans is false, v->nb[1] == row_size(head_dim)
+            // and positions live at v->nb[2]. When v_trans is true,
+            // v->nb[1] > v->nb[2] (positions innermost).
+            const bool v_contig_per_pos = (size_t)v->nb[1] <= (size_t)v->nb[2];
+
+            if (v_contig_per_pos) {
+                const size_t n_embd_v_gqa = (size_t)v->ne[0]; // head_dim * nhk
+                if (n_embd_v_gqa == (size_t)hd_v * (size_t)nhk) {
+                    const size_t v_pos_stride = n_embd_v_gqa * ev;
+                    uint8_t * vdata = (uint8_t *)v->data;
+
+                    for (int t = 0; t < n_tokens; ++t) {
+                        uint32_t slot = sinfo.idxs[0][t];
+                        const size_t pos_off = (size_t)slot * v_pos_stride;
+
+                        for (int h = 0; h < nhk; ++h) {
+                            const size_t off = pos_off + (size_t)h * hd_v * ev;
+                            if (!gather_vec(vdata, off, hd_v, tv, gather)) continue;
+
+                            sp_llama_write_v(g_sp.ctx, il, h, 0, gather);
+                            sp_llama_read_v (g_sp.ctx, il, h, 0, recon);
+
+                            scatter_vec(vdata, off, hd_v, tv, recon);
+                        }
+                    }
+                }
+            } else {
+                // v_trans=true path — vector for (h, pos) is strided by
+                // kv_size elements per head_dim component. Out of scope
+                // for the first post-decode hook; document.
+                static std::atomic<bool> warned{false};
+                bool expected = false;
+                if (warned.compare_exchange_strong(expected, true)) {
+                    std::fprintf(stderr,
+                        "[Shannon-Prime] V cache has v_trans=true layout; "
+                        "V round-trip is not yet implemented in the post-decode hook. "
+                        "K is still round-tripped. Set cache_type_v to the non-transposed "
+                        "layout when creating the context to enable V as well.\n");
+                }
+            }
+        }
+    }
+}
+
+#endif // LLAMA_SHANNON_PRIME
diff --git a/src/llama-shannon-prime.h b/src/llama-shannon-prime.h
new file mode 100644
index 0000000..44f44aa
--- /dev/null
+++ b/src/llama-shannon-prime.h
@@ -0,0 +1,53 @@
+// Shannon-Prime VHT2 KV cache compression hooks for llama.cpp.
+//
+// Activated when SHANNON_PRIME_ENABLED=1 is set in the environment.
+// Hook strategy: a post-graph_compute callback that walks the freshly-
+// written slots of the llama KV cache (layers[il].k / .v) and round-trips
+// their data through the Shannon-Prime compressed shadow cache in place.
+// Attention later reads the cache tensors directly, so seeing
+// reconstructed-from-compressed values. Single sync per decode call
+// instead of per-tensor — avoids the ggml_backend_sched eval-callback
+// serialization that killed throughput on mobile.
+//
+// CPU-only. Handles fp32 and fp16 KV cache types; other types are
+// passed through untouched (one-shot warning).
+
+#pragma once
+
+#ifdef LLAMA_SHANNON_PRIME
+
+#include <stdbool.h>
+#include "ggml.h"
+
+struct llama_model;
+struct llama_ubatch;
+struct llama_memory_context_i;
+
+#ifdef __cplusplus
+extern "C" {
+#endif
+
+// Lazily initialise the global Shannon-Prime context from model hparams +
+// environment variables. No-op if already initialised or if
+// SHANNON_PRIME_ENABLED is not set.
+void llama_sp_maybe_init(const struct llama_model * model, int n_ctx);
+
+// Tear down the global context. Idempotent.
+void llama_sp_free(void);
+
+// True iff Shannon-Prime is active.
+bool llama_sp_is_enabled(void);
+
+// Post-decode hook. Called from llama_context::process_ubatch right after
+// graph_compute returns successfully. Walks the KV cache tensor storage
+// for the positions just written by this ubatch, round-trips K (and V
+// when its layout is contiguous per-position) through the Shannon-Prime
+// shadow cache, and writes the reconstructed values back in place.
+void llama_sp_post_compute(const struct llama_memory_context_i * mctx,
+                           const struct llama_ubatch * ubatch);
+
+#ifdef __cplusplus
+}
+#endif
+
+#endif // LLAMA_SHANNON_PRIME
